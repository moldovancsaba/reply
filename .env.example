# Copy to `.env` and fill in. Repo-root `.env` is for local automation only (not loaded by `chat/server.js`).
# Classic PAT (moldovancsaba): repo + read:project + project
# https://github.com/settings/tokens

GITHUB_TOKEN=

# Optional: split mode for migrate-reply-github-board.mjs
# CHAPPIE_TOKEN=
# OWNER_TOKEN=
